"""
Silver → Gold — Transform & Enrichment.

Derives assumption-setting-relevant metrics from validated (PASS/DEGRADED)
records.

TODO:
- calculate_policy_duration(df) -> DataFrame
- flag_lapse(df) -> DataFrame
- lapse_rate_by_segment(df, segment_col) -> DataFrame
"""
